# Remotion workspace

The Remotion composition will be introduced in Phase 4, after transcript timing and caption segmentation have been measured and verified. Keeping it isolated here prevents preview/render code from leaking into the editor UI.

