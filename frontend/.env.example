# For Vercel, set VITE_API_URL in the project environment to the HTTPS address
# of the separately hosted processing API. Do not append /api.
# VITE_API_URL=https://api.example.com

# The local web app uses Vite's /api proxy when this variable is unset.
# Android debug builds let you enter the LAN address inside the app.
